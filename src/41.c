#include <stdio.h>

// Function to perform subtraction
int subtract(int a, int b) {
    return a - b;
}

// Main function
int main() {
    printf("Subtraction: %d - %d = %d\n", 10, 5, 5);
    return 0;
}
